<!doctype html>
<html>
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1"/>
<title>Ticket <%= bet.ref %></title>
<style>
body{font-family:system-ui,-apple-system,Segoe UI,Roboto,Inter,Arial,sans-serif;max-width:360px;margin:0 auto;padding:16px;background:#fff;color:#111}
header{ text-align:center; }
h1{ font-size:18px; margin:0; }
.muted{ color:#555; font-size:12px; }
.line{ border-top:1px dashed #bbb; margin:8px 0; }
.row{ display:flex; justify-content:space-between; margin:4px 0; }
.money{ font-weight:700; }
.center{ text-align:center; }
.barcode{ display:block; margin:10px auto; }
.big{ font-size:20px; font-weight:800; }
</style>
</head>
<body>
<header>
<h1>MASTERMIND BET</h1>
<div class="muted"><%= DOMAIN %></div>
</header>
<div class="line"></div>
<div class="row"><div>Ticket</div><div><b><%= bet.ref %></b></div></div>
<div class="row"><div>Game</div><div><%= event.game.toUpperCase() %><% if (event.league) { %> (<%= event.league %>)<% } %> — <%= event.id.slice(0,8) %></div></div>
<div class="row"><div>Market</div><div><%= market.type %></div></div>
<div class="row"><div>Selection</div><div><%= bet.selectionId %> @ <%= bet.odds %></div></div>
<div class="row"><div>Stake</div><div class="money"><%= fmtMoney(bet.stake) %></div></div>
<div class="row"><div>Placed</div><div><%= new Date(bet.placedAt).toLocaleString('en-KE') %></div></div>
<div class="line"></div>
<div class="center"><img alt="barcode" class="barcode" src="<%= barcodeBase64 %>"/></div>
<div class="center muted">Keep your ticket safe</div>
</body>
</html>
